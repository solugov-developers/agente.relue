import type { Metadata } from "next";
import RelueChat from "@/components/RelueChat";

export const metadata: Metadata = { title: "Relue IA · Inteligência de Licitações" };

export default function Page() {
  return <RelueChat />;
}
